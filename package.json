{
  "name": "contract",
  "description": "seiyan fun smart contracts",
  "scripts": {
    "lint:sol": "prettier --log-level warn --ignore-path .gitignore '{src,script,test}/**/*.sol' --check",
    "lint:sol:fix": "prettier --log-level warn --ignore-path .gitignore '{src,script,test}/**/*.sol' --write"
  },
  "devDependencies": {
    "prettier": "^3.3.2",
    "prettier-plugin-solidity": "^1.3.1",
    "viem": "^2.18.7"
  }
}
